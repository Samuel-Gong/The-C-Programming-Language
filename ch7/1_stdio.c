#include <stdio.h>
#include <ctype.h>

/* lower 函数：将输入转换为小写形式 */
int main() {
    int c = 'A';
    printf("%c", tolower(c));

    // while((c = getchar()) != EOF) {
        // putchar(tolower(c));
    // }
    return 0;
}